import React, { memo } from "react";
import "./index.scss";

const Header = () => {
  return (
    <nav className="root-header">
      <div className="root-header-main flex-center flex-end">dsadas</div>
    </nav>
  );
};

export default memo(Header);
